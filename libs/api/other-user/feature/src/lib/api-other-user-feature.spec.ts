import { apiOtherUserFeature } from './api-other-user-feature';

describe('apiOtherUserFeature', () => {
  it('should work', () => {
    expect(apiOtherUserFeature()).toEqual('api-other-user-feature');
  });
});
